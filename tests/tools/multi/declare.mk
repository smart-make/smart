smart.test.load.before.push = $(shell echo "0:$(NAME),$(TOOL),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> loads.log)
smart.test.load.after.push = $(shell echo "1:$(NAME),$(TOOL),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> loads.log)
smart.test.loaded = $(shell echo "2:$(NAME),$(TOOL),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> loads.log)
smart.test.load.before.pop = $(shell echo "3:$(NAME),$(TOOL),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> loads.log)
smart.test.load.after.pop = $(shell echo "4:$(NAME),$(TOOL),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> loads.log)
test-log-visit = $(shell echo "$(NAME),$(call smart.get,$(NAME),NAME),$(SRCDIR),$(SM.MK),$(lastword $(MAKEFILE_LIST))" >> visit.log)
test-log-tool = $(shell echo "$(SRCDIR),$(lastword $(MAKEFILE_LIST)),$(TOOL),$(call smart.get,$(NAME),TOOL)" >> tools.log)
discard := $(test-log-visit)
discard := $(test-log-tool)
